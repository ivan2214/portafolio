---
type Props = {
  alt: string
  src: any
  clases?: string
}
const { clases, src, alt } = Astro.props
---

<img
  class={`w-6 h-6 ${clases}`}
  src={src}
  alt={alt}
  width={750}
  loading="lazy"
  class='max-w-[2.5rem] object-cover'
/>
