---
import Paragraph from './Paragraph.astro'
import { Image } from '@astrojs/image/components'

export interface Props {
  title: string
  description: string
  img: any
}

const { title, description, img } = Astro.props
---

<!-- <div class='rounded-lg bg-base-100 hover:shadow-xl transition ease-in-out'>
  <div class='hero-content flex-col items-start gap-10'>
    {
      img && (
        <Image
          src={img || 'https://via.placeholder.com/300x300?text='}
          aspectRatio='16:9'
          width={750}
          format='webp'
          alt={title}
          class='max-w-[3rem] object-cover rounded-lg'
        />
      )
    }
    <div class='grow w-full'>
      <h2 class='text-xl font-bold'>
        {title}
      </h2>
      <p class='py-1 text-1xl'>{description}</p>
    </div>
  </div>
</div> -->
<div
  class='w-full lg:start-96 border border-secondary p-6 shadow rounded-lg bg-base-100 hover:shadow-2xl transition ease-in-out lg:min-h-full lg:max-h-max lg:max-w-xs flex items-start flex-col gap-5'
>
  {
    img && (
      <Image
        src={img || 'https://via.placeholder.com/300x300?text='}
        aspectRatio='16:16'
        width={750}
        format='webp'
        alt={title}
        class='max-w-[2.5rem] object-cover'
      />
    )
  }

  <div class='flex flex-col items-start gap-2'>
    <h2 class='ext-xl font-bold'>
      {title}
    </h2>

    <p class='py-1 text-1xl'>
      {description}
    </p>
  </div>
</div>
